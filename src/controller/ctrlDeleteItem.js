import {
  domAddBtn,
  domAddType,
  domAddValue,
  domAddDescription
} from '../view/elements';

import * as budjetCtrl from '../model/budjetModel.js';
import * as uiCtrl from '../view/budjetUI.js';

export function ctrlDeleteItem(event) {
  // 1 - read the id of the item from the DOM data-id attribute

  // 2 - remove the item from the budjet model

  // 3 - remove the item from the UI 
}

function deleteIncome() {

}

function deleteExpense() {

}